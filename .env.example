# Copy to .env for local development. Never commit .env.

# --- Database ---
POSTGRES_USER=loanflow
POSTGRES_PASSWORD=loanflow
POSTGRES_DB=loanflow
# Consumed by api + worker. Host "db" is the compose service name.
DATABASE_URL=postgresql+psycopg://loanflow:loanflow@db:5432/loanflow

# --- API ---
# dev | test | local skip the strong-secret check below; anything else requires
# a real JWT_SECRET (>=32 chars) or the API refuses to start.
ENVIRONMENT=dev
# Generate: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=dev-only-change-me
JWT_ALGORITHM=HS256
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_DAYS=14
CORS_ORIGINS=http://localhost:5173
LOG_LEVEL=INFO

# --- Worker ---
PURGE_AFTER_DAYS=30
HOUSEKEEPING_CRON=0 2 * * *

# --- Web (build-time) ---
VITE_API_BASE_URL=/api
